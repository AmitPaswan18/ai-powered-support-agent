{
  "name": "fastapi-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn api.index:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python -m uvicorn api.index:app --host 0.0.0.0 --port 8000"
  }
}